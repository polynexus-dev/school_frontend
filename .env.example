# Base URL of the School Edition backend API, including the /api/ path prefix.
# The axios instance in src/services/api.jsx appends relative paths like
# "token/", "students/", "profile/" onto this base.
VITE_API_URL=http://localhost:8000/api/
